<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Maurice.Views.BuscarFactura"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Maurice.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
		Width="400"
        Height="250">

    <StackPanel Margin="15">
        <TextBlock Text="Buscar por:" />
        <ComboBox 
			ItemsSource="{Binding SearchOptions}"
			SelectedIndex="0"
			MaxDropDownHeight="300"
			Margin="1"
			Width="370">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding OptionName}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>


        <Button Content="Buscar" Command="{Binding Buscar}" Margin="0,20" />
    </StackPanel>
</Page>
